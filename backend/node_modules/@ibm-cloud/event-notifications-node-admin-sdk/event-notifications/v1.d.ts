/**
 * (C) Copyright IBM Corp. 2025.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import { IncomingHttpHeaders, OutgoingHttpHeaders } from 'http';
import { BaseService, UserOptions } from 'ibm-cloud-sdk-core';
/**
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * API Version: 1.0
 */
declare class EventNotificationsV1 extends BaseService {
    static DEFAULT_SERVICE_URL: string;
    static DEFAULT_SERVICE_NAME: string;
    /*************************
     * Factory method
     ************************/
    /**
     * Constructs an instance of EventNotificationsV1 with passed in options and external configuration.
     *
     * @param {UserOptions} [options] - The parameters to send to the service.
     * @param {string} [options.serviceName] - The name of the service to configure
     * @param {Authenticator} [options.authenticator] - The Authenticator object used to authenticate requests to the service
     * @param {string} [options.serviceUrl] - The URL for the service
     * @returns {EventNotificationsV1}
     */
    static newInstance(options: UserOptions): EventNotificationsV1;
    /**
     * Construct a EventNotificationsV1 object.
     *
     * @param {Object} options - Options for the service.
     * @param {string} [options.serviceUrl] - The base url to use when contacting the service. The base url may differ between IBM Cloud regions.
     * @param {OutgoingHttpHeaders} [options.headers] - Default headers that shall be included with every request to the service.
     * @param {Authenticator} options.authenticator - The Authenticator object used to authenticate requests to the service
     * @constructor
     * @returns {EventNotificationsV1}
     */
    constructor(options: UserOptions);
    /*************************
     * metrics
     ************************/
    /**
     * Get metrics.
     *
     * Get metrics.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.destinationType - Destination type. Allowed values are [smtp_custom].
     * @param {string} params.gte - GTE (greater than equal), start timestamp in UTC.
     * @param {string} params.lte - LTE (less than equal), end timestamp in UTC.
     * @param {string} [params.destinationId] - Unique identifier for Destination.
     * @param {string} [params.sourceId] - Unique identifier for Source.
     * @param {string} [params.emailTo] - Receiver email id.
     * @param {string} [params.notificationId] - Notification Id.
     * @param {string} [params.subject] - Email subject.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Metrics>>}
     */
    getMetrics(params: EventNotificationsV1.GetMetricsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Metrics>>;
    /*************************
     * sendNotifications
     ************************/
    /**
     * Send a notification.
     *
     * Send Notifications body from the instance. For more information about Event Notifications payload, see
     * [here](https://cloud.ibm.com/docs/event-notifications?topic=event-notifications-en-spec-payload).
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {NotificationCreate} [params.body] -
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.NotificationResponse>>}
     */
    sendNotifications(params: EventNotificationsV1.SendNotificationsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.NotificationResponse>>;
    /*************************
     * sources
     ************************/
    /**
     * Create a new API Source.
     *
     * Create a new API Source.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - Name of the source.
     * @param {string} params.description - Description of the source.
     * @param {boolean} [params.enabled] - Whether the source is enabled or not.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SourceResponse>>}
     */
    createSources(params: EventNotificationsV1.CreateSourcesParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SourceResponse>>;
    /**
     * List all Sources.
     *
     * List all Sources.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SourceList>>}
     */
    listSources(params: EventNotificationsV1.ListSourcesParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SourceList>>;
    /**
     * Get a Source.
     *
     * Get a Source.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Source.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Source>>}
     */
    getSource(params: EventNotificationsV1.GetSourceParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Source>>;
    /**
     * Delete a Source.
     *
     * Delete a Source.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Source.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteSource(params: EventNotificationsV1.DeleteSourceParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * Update details of a Source.
     *
     * Update details of a Source.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Source.
     * @param {string} [params.name] - Name of the source.
     * @param {string} [params.description] - Description of the source.
     * @param {boolean} [params.enabled] - Whether the source is enabled or not.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Source>>}
     */
    updateSource(params: EventNotificationsV1.UpdateSourceParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Source>>;
    /*************************
     * topics
     ************************/
    /**
     * Create a new Topic.
     *
     * Create a new Topic.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - Name of the topic.
     * @param {string} [params.description] - Description of the topic.
     * @param {SourcesItems[]} [params.sources] - List of sources.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TopicResponse>>}
     */
    createTopic(params: EventNotificationsV1.CreateTopicParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TopicResponse>>;
    /**
     * List all Topics.
     *
     * List all Topics.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TopicList>>}
     */
    listTopics(params: EventNotificationsV1.ListTopicsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TopicList>>;
    /**
     * Get details of a Topic.
     *
     * Get details of a Topic.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Topic.
     * @param {string} [params.include] - Include sub topics.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Topic>>}
     */
    getTopic(params: EventNotificationsV1.GetTopicParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Topic>>;
    /**
     * Update details of a Topic.
     *
     * Update details of a Topic.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Topic.
     * @param {string} [params.name] - Name of the topic.
     * @param {string} [params.description] - Description of the topic.
     * @param {SourcesItems[]} [params.sources] - List of sources.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Topic>>}
     */
    replaceTopic(params: EventNotificationsV1.ReplaceTopicParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Topic>>;
    /**
     * Delete a Topic.
     *
     * Delete a Topic.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Topic.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteTopic(params: EventNotificationsV1.DeleteTopicParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /*************************
     * templates
     ************************/
    /**
     * Create a new Template.
     *
     * Create a new Template.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - The Message Template.
     * @param {string} params.type - The type of template.
     * @param {TemplateConfigOneOf} params.params -
     * @param {string} [params.description] - The Template description.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TemplateResponse>>}
     */
    createTemplate(params: EventNotificationsV1.CreateTemplateParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TemplateResponse>>;
    /**
     * List all templates.
     *
     * List all Templates.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TemplateList>>}
     */
    listTemplates(params: EventNotificationsV1.ListTemplatesParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TemplateList>>;
    /**
     * Get details of a Template.
     *
     * Get details of a Template.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Template.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Template>>}
     */
    getTemplate(params: EventNotificationsV1.GetTemplateParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Template>>;
    /**
     * Update details of a Template.
     *
     * Update details of a Template.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Template.
     * @param {string} [params.name] - Template name.
     * @param {string} [params.description] - Template description.
     * @param {string} [params.type] - The type of template.
     * @param {TemplateConfigOneOf} [params.params] -
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Template>>}
     */
    replaceTemplate(params: EventNotificationsV1.ReplaceTemplateParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Template>>;
    /**
     * Delete a Template.
     *
     * Delete a Template.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Template.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteTemplate(params: EventNotificationsV1.DeleteTemplateParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * List all predefined templates.
     *
     * List all predefined templates.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.source - Source type.
     * @param {string} params.type - Destination type.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.PredefinedTemplatesList>>}
     */
    listPreDefinedTemplates(params: EventNotificationsV1.ListPreDefinedTemplatesParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.PredefinedTemplatesList>>;
    /**
     * Get details of a Predefined Template.
     *
     * Get details of a Predefined Template.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Template.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.GetPredefinedTemplate>>}
     */
    getPreDefinedTemplate(params: EventNotificationsV1.GetPreDefinedTemplateParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.GetPredefinedTemplate>>;
    /*************************
     * destinations
     ************************/
    /**
     * Create a new Destination.
     *
     * Create a new Destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - The Destination name.
     * @param {string} params.type - The type of Destination Webhook.
     * @param {string} [params.description] - The Destination description.
     * @param {boolean} [params.collectFailedEvents] - Whether to collect the failed event in Cloud Object Storage bucket.
     * @param {DestinationConfig} [params.config] - Payload describing a destination configuration.
     * @param {NodeJS.ReadableStream | Buffer} [params.certificate] - Certificate for APNS.
     * @param {string} [params.certificateContentType] - The content type of certificate.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon16x16] - Safari icon 16x16.
     * @param {string} [params.icon16x16ContentType] - The content type of icon16x16.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon16x162x] - Safari icon 16x16@2x.
     * @param {string} [params.icon16x162xContentType] - The content type of icon16x162x.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon32x32] - Safari icon 32x32.
     * @param {string} [params.icon32x32ContentType] - The content type of icon32x32.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon32x322x] - Safari icon 32x32@2x.
     * @param {string} [params.icon32x322xContentType] - The content type of icon32x322x.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon128x128] - Safari icon 128x128.
     * @param {string} [params.icon128x128ContentType] - The content type of icon128x128.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon128x1282x] - Safari icon 128x128@2x.
     * @param {string} [params.icon128x1282xContentType] - The content type of icon128x1282x.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationResponse>>}
     */
    createDestination(params: EventNotificationsV1.CreateDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationResponse>>;
    /**
     * List all Destinations.
     *
     * List all Destinations.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationList>>}
     */
    listDestinations(params: EventNotificationsV1.ListDestinationsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationList>>;
    /**
     * Get details of a Destination.
     *
     * Get details of a Destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Destination>>}
     */
    getDestination(params: EventNotificationsV1.GetDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Destination>>;
    /**
     * Update details of a Destination.
     *
     * Update details of a Destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {string} [params.name] - Destination name.
     * @param {string} [params.description] - Destination description.
     * @param {boolean} [params.collectFailedEvents] - Whether to collect the failed event in Cloud Object Storage bucket.
     * @param {DestinationConfig} [params.config] - Payload describing a destination configuration.
     * @param {NodeJS.ReadableStream | Buffer} [params.certificate] - Certificate for APNS.
     * @param {string} [params.certificateContentType] - The content type of certificate.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon16x16] - Safari icon 16x16.
     * @param {string} [params.icon16x16ContentType] - The content type of icon16x16.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon16x162x] - Safari icon 16x16@2x.
     * @param {string} [params.icon16x162xContentType] - The content type of icon16x162x.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon32x32] - Safari icon 32x32.
     * @param {string} [params.icon32x32ContentType] - The content type of icon32x32.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon32x322x] - Safari icon 32x32@2x.
     * @param {string} [params.icon32x322xContentType] - The content type of icon32x322x.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon128x128] - Safari icon 128x128.
     * @param {string} [params.icon128x128ContentType] - The content type of icon128x128.
     * @param {NodeJS.ReadableStream | Buffer} [params.icon128x1282x] - Safari icon 128x128@2x.
     * @param {string} [params.icon128x1282xContentType] - The content type of icon128x1282x.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Destination>>}
     */
    updateDestination(params: EventNotificationsV1.UpdateDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Destination>>;
    /**
     * Delete a Destination.
     *
     * Delete a Destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteDestination(params: EventNotificationsV1.DeleteDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * Get enabled country details of SMS destination.
     *
     * Get enabled country details of SMS destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EnabledCountriesResponse>>}
     */
    getEnabledCountries(params: EventNotificationsV1.GetEnabledCountriesParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EnabledCountriesResponse>>;
    /**
     * Test a Destination.
     *
     * Test a Destination.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TestDestinationResponse>>}
     */
    testDestination(params: EventNotificationsV1.TestDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TestDestinationResponse>>;
    /**
     * Verify SPF and DKIM records of custom domain.
     *
     * Verify SPF and DKIM records of custom domain.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {string} params.type - Verification type.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.VerificationResponse>>}
     */
    updateVerifyDestination(params: EventNotificationsV1.UpdateVerifyDestinationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.VerificationResponse>>;
    /*************************
     * pushDestinationAPIs
     ************************/
    /**
     * Create a new tag subscription.
     *
     * Create a new tag subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {string} params.deviceId - Unique identifier of the device.
     * @param {string} params.tagName - The name of the tag its subscribed.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationTagsSubscriptionResponse>>}
     */
    createTagsSubscription(params: EventNotificationsV1.CreateTagsSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.DestinationTagsSubscriptionResponse>>;
    /**
     * List all tag subscriptions.
     *
     * List all tag subscriptions.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {string} [params.deviceId] - Device ID of the destination tagsubscription.
     * @param {string} [params.userId] - UserID of the destination.
     * @param {string} [params.tagName] - TagName of the subscription.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.TagsSubscriptionList>>}
     */
    listTagsSubscription(params: EventNotificationsV1.ListTagsSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.TagsSubscriptionList>>;
    /**
     * Delete a tag subscription.
     *
     * Delete a tag subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Destination.
     * @param {string} [params.deviceId] - Device ID of the destination tagsubscription.
     * @param {string} [params.tagName] - TagName of the subscription.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteTagsSubscription(params: EventNotificationsV1.DeleteTagsSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /*************************
     * subscriptions
     ************************/
    /**
     * Create a new Subscription.
     *
     * Create a new Subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - Subscription name.
     * @param {string} params.destinationId - Destination ID.
     * @param {string} params.topicId - Topic ID.
     * @param {string} [params.description] - Subscription description.
     * @param {SubscriptionCreateAttributes} [params.attributes] -
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>}
     */
    createSubscription(params: EventNotificationsV1.CreateSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>;
    /**
     * List all Subscriptions.
     *
     * List all Subscriptions.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SubscriptionList>>}
     */
    listSubscriptions(params: EventNotificationsV1.ListSubscriptionsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SubscriptionList>>;
    /**
     * Get details of a Subscription.
     *
     * Get details of a Subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Subscription.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>}
     */
    getSubscription(params: EventNotificationsV1.GetSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>;
    /**
     * Delete a Subscription.
     *
     * Delete a Subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Subscription.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteSubscription(params: EventNotificationsV1.DeleteSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * Update details of a Subscription.
     *
     * Update details of a Subscription.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for Subscription.
     * @param {string} [params.name] - Name of the subscription.
     * @param {string} [params.description] - Description of the subscription.
     * @param {SubscriptionUpdateAttributes} [params.attributes] -
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>}
     */
    updateSubscription(params: EventNotificationsV1.UpdateSubscriptionParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.Subscription>>;
    /*************************
     * integrations
     ************************/
    /**
     * Create an Integration.
     *
     * Create an Integration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.type - The type of Integration.
     * @param {IntegrationCreateMetadata} params.metadata - Integration Metadata object.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationCreateResponse>>}
     */
    createIntegration(params: EventNotificationsV1.CreateIntegrationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationCreateResponse>>;
    /**
     * List all Integrations.
     *
     * List of all KMS Integrations.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationList>>}
     */
    listIntegrations(params: EventNotificationsV1.ListIntegrationsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationList>>;
    /**
     * Get a single Integration.
     *
     * Get a single KMS Integration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for integration.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationGetResponse>>}
     */
    getIntegration(params: EventNotificationsV1.GetIntegrationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationGetResponse>>;
    /**
     * Update an existing Integration.
     *
     * Update an existing Integration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for integration.
     * @param {string} params.type - Integration type. Allowed values are kms, hs-crypto and collect_failed_events.
     * @param {IntegrationMetadata} params.metadata - Integration Metadata object.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationGetResponse>>}
     */
    replaceIntegration(params: EventNotificationsV1.ReplaceIntegrationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.IntegrationGetResponse>>;
    /*************************
     * sMTPConfigurations
     ************************/
    /**
     * Create a new SMTP Configuration.
     *
     * Create a new SMTP Configuration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.name - The name of SMTP configuration.
     * @param {string} params.domain - Domain Name.
     * @param {string} [params.description] - The description of SMTP configuration.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPCreateResponse>>}
     */
    createSmtpConfiguration(params: EventNotificationsV1.CreateSmtpConfigurationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPCreateResponse>>;
    /**
     * List all SMTP Configurations.
     *
     * List all SMTP Configurations.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfigurationsList>>}
     */
    listSmtpConfigurations(params: EventNotificationsV1.ListSmtpConfigurationsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfigurationsList>>;
    /**
     * Create a new SMTP User.
     *
     * Create a new SMTP User.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} [params.description] - The description of SMTP configuration.
     * @param {string} [params.usernameToClone] - provide name of the user to clone.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUserResponse>>}
     */
    createSmtpUser(params: EventNotificationsV1.CreateSmtpUserParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUserResponse>>;
    /**
     * List all SMTP users.
     *
     * List all SMTP users.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {number} [params.limit] - Page limit for paginated results.
     * @param {number} [params.offset] - offset for paginated results.
     * @param {string} [params.search] - Search string for filtering results.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUsersList>>}
     */
    listSmtpUsers(params: EventNotificationsV1.ListSmtpUsersParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUsersList>>;
    /**
     * Get details of a SMTP Configuration.
     *
     * Get details of a SMTP Configuration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfiguration>>}
     */
    getSmtpConfiguration(params: EventNotificationsV1.GetSmtpConfigurationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfiguration>>;
    /**
     * Update details of SMTP Configuration.
     *
     * Update details of SMTP Configuration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} [params.name] - SMTP name.
     * @param {string} [params.description] - SMTP description.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfiguration>>}
     */
    updateSmtpConfiguration(params: EventNotificationsV1.UpdateSmtpConfigurationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPConfiguration>>;
    /**
     * Delete a SMTP Configuration.
     *
     * Delete a SMTP Configuration.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteSmtpConfiguration(params: EventNotificationsV1.DeleteSmtpConfigurationParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * Get details of a SMTP User.
     *
     * Get details of a SMTP User.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} params.userId - UserID.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUser>>}
     */
    getSmtpUser(params: EventNotificationsV1.GetSmtpUserParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUser>>;
    /**
     * Update details of a SMTP User.
     *
     * Update details of a SMTP User.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} params.userId - UserID.
     * @param {string} [params.description] - SMTP user description.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUser>>}
     */
    updateSmtpUser(params: EventNotificationsV1.UpdateSmtpUserParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPUser>>;
    /**
     * Delete a SMTP user.
     *
     * Delete a SMTP user.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} params.userId - UserID.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>}
     */
    deleteSmtpUser(params: EventNotificationsV1.DeleteSmtpUserParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.EmptyObject>>;
    /**
     * Get details of SMTP configuration allowed IPs.
     *
     * Get details of SMTP configuration allowed IPs.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPAllowedIPs>>}
     */
    getSmtpAllowedIps(params: EventNotificationsV1.GetSmtpAllowedIpsParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPAllowedIPs>>;
    /**
     * Verify SMTP configuration domain.
     *
     * Verify SMTP configuration domain.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Unique identifier for SMTP.
     * @param {string} params.type - SMTP verification type.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPVerificationUpdateResponse>>}
     */
    updateVerifySmtp(params: EventNotificationsV1.UpdateVerifySmtpParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.SMTPVerificationUpdateResponse>>;
    /*************************
     * getNotificationsStatus
     ************************/
    /**
     * Get notification status.
     *
     * Get notification status.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - Unique identifier for IBM Cloud Event Notifications instance.
     * @param {string} params.id - Get Notification Id.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<EventNotificationsV1.Response<EventNotificationsV1.GetNotificationStatusResponse>>}
     */
    getNotificationsStatus(params: EventNotificationsV1.GetNotificationsStatusParams): Promise<EventNotificationsV1.Response<EventNotificationsV1.GetNotificationStatusResponse>>;
}
/*************************
 * interfaces
 ************************/
declare namespace EventNotificationsV1 {
    /** An operation response. */
    interface Response<T = any> {
        result: T;
        status: number;
        statusText: string;
        headers: IncomingHttpHeaders;
    }
    /** The callback for a service request. */
    type Callback<T> = (error: any, response?: Response<T>) => void;
    /** The body of a service request that returns no response data. */
    interface EmptyObject {
    }
    /** A standard JS object, defined to avoid the limitations of `Object` and `object` */
    interface JsonObject {
        [key: string]: any;
    }
    /*************************
     * request interfaces
     ************************/
    /** Parameters for the `getMetrics` operation. */
    interface GetMetricsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Destination type. Allowed values are [smtp_custom]. */
        destinationType: GetMetricsConstants.DestinationType | string;
        /** GTE (greater than equal), start timestamp in UTC. */
        gte: string;
        /** LTE (less than equal), end timestamp in UTC. */
        lte: string;
        /** Unique identifier for Destination. */
        destinationId?: string;
        /** Unique identifier for Source. */
        sourceId?: string;
        /** Receiver email id. */
        emailTo?: string;
        /** Notification Id. */
        notificationId?: string;
        /** Email subject. */
        subject?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Constants for the `getMetrics` operation. */
    namespace GetMetricsConstants {
        /** Destination type. Allowed values are [smtp_custom]. */
        enum DestinationType {
            SMTP_CUSTOM = "smtp_custom"
        }
    }
    /** Parameters for the `sendNotifications` operation. */
    interface SendNotificationsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        body?: NotificationCreate;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createSources` operation. */
    interface CreateSourcesParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Name of the source. */
        name: string;
        /** Description of the source. */
        description: string;
        /** Whether the source is enabled or not. */
        enabled?: boolean;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listSources` operation. */
    interface ListSourcesParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getSource` operation. */
    interface GetSourceParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Source. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteSource` operation. */
    interface DeleteSourceParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Source. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateSource` operation. */
    interface UpdateSourceParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Source. */
        id: string;
        /** Name of the source. */
        name?: string;
        /** Description of the source. */
        description?: string;
        /** Whether the source is enabled or not. */
        enabled?: boolean;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createTopic` operation. */
    interface CreateTopicParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Name of the topic. */
        name: string;
        /** Description of the topic. */
        description?: string;
        /** List of sources. */
        sources?: SourcesItems[];
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listTopics` operation. */
    interface ListTopicsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getTopic` operation. */
    interface GetTopicParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Topic. */
        id: string;
        /** Include sub topics. */
        include?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `replaceTopic` operation. */
    interface ReplaceTopicParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Topic. */
        id: string;
        /** Name of the topic. */
        name?: string;
        /** Description of the topic. */
        description?: string;
        /** List of sources. */
        sources?: SourcesItems[];
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteTopic` operation. */
    interface DeleteTopicParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Topic. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createTemplate` operation. */
    interface CreateTemplateParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** The Message Template. */
        name: string;
        /** The type of template. */
        type: string;
        params: TemplateConfigOneOf;
        /** The Template description. */
        description?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listTemplates` operation. */
    interface ListTemplatesParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getTemplate` operation. */
    interface GetTemplateParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Template. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `replaceTemplate` operation. */
    interface ReplaceTemplateParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Template. */
        id: string;
        /** Template name. */
        name?: string;
        /** Template description. */
        description?: string;
        /** The type of template. */
        type?: string;
        params?: TemplateConfigOneOf;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteTemplate` operation. */
    interface DeleteTemplateParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Template. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listPreDefinedTemplates` operation. */
    interface ListPreDefinedTemplatesParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Source type. */
        source: string;
        /** Destination type. */
        type: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getPreDefinedTemplate` operation. */
    interface GetPreDefinedTemplateParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Template. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createDestination` operation. */
    interface CreateDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** The Destination name. */
        name: string;
        /** The type of Destination Webhook. */
        type: CreateDestinationConstants.Type | string;
        /** The Destination description. */
        description?: string;
        /** Whether to collect the failed event in Cloud Object Storage bucket. */
        collectFailedEvents?: boolean;
        /** Payload describing a destination configuration. */
        config?: DestinationConfig;
        /** Certificate for APNS. */
        certificate?: NodeJS.ReadableStream | Buffer;
        /** The content type of certificate. */
        certificateContentType?: string;
        /** Safari icon 16x16. */
        icon16x16?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon16x16. */
        icon16x16ContentType?: string;
        /** Safari icon 16x16@2x. */
        icon16x162x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon16x162x. */
        icon16x162xContentType?: string;
        /** Safari icon 32x32. */
        icon32x32?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon32x32. */
        icon32x32ContentType?: string;
        /** Safari icon 32x32@2x. */
        icon32x322x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon32x322x. */
        icon32x322xContentType?: string;
        /** Safari icon 128x128. */
        icon128x128?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon128x128. */
        icon128x128ContentType?: string;
        /** Safari icon 128x128@2x. */
        icon128x1282x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon128x1282x. */
        icon128x1282xContentType?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Constants for the `createDestination` operation. */
    namespace CreateDestinationConstants {
        /** The type of Destination Webhook. */
        enum Type {
            WEBHOOK = "webhook",
            PUSH_ANDROID = "push_android",
            PUSH_IOS = "push_ios",
            PUSH_CHROME = "push_chrome",
            PUSH_FIREFOX = "push_firefox",
            SLACK = "slack",
            IBMCE = "ibmce",
            PAGERDUTY = "pagerduty",
            PUSH_SAFARI = "push_safari",
            MSTEAMS = "msteams",
            SERVICENOW = "servicenow",
            IBMCOS = "ibmcos",
            PUSH_HUAWEI = "push_huawei",
            SMTP_CUSTOM = "smtp_custom",
            SMS_CUSTOM = "sms_custom",
            EVENT_STREAMS = "event_streams",
            APP_CONFIGURATION = "app_configuration"
        }
    }
    /** Parameters for the `listDestinations` operation. */
    interface ListDestinationsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getDestination` operation. */
    interface GetDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateDestination` operation. */
    interface UpdateDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        /** Destination name. */
        name?: string;
        /** Destination description. */
        description?: string;
        /** Whether to collect the failed event in Cloud Object Storage bucket. */
        collectFailedEvents?: boolean;
        /** Payload describing a destination configuration. */
        config?: DestinationConfig;
        /** Certificate for APNS. */
        certificate?: NodeJS.ReadableStream | Buffer;
        /** The content type of certificate. */
        certificateContentType?: string;
        /** Safari icon 16x16. */
        icon16x16?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon16x16. */
        icon16x16ContentType?: string;
        /** Safari icon 16x16@2x. */
        icon16x162x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon16x162x. */
        icon16x162xContentType?: string;
        /** Safari icon 32x32. */
        icon32x32?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon32x32. */
        icon32x32ContentType?: string;
        /** Safari icon 32x32@2x. */
        icon32x322x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon32x322x. */
        icon32x322xContentType?: string;
        /** Safari icon 128x128. */
        icon128x128?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon128x128. */
        icon128x128ContentType?: string;
        /** Safari icon 128x128@2x. */
        icon128x1282x?: NodeJS.ReadableStream | Buffer;
        /** The content type of icon128x1282x. */
        icon128x1282xContentType?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteDestination` operation. */
    interface DeleteDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getEnabledCountries` operation. */
    interface GetEnabledCountriesParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `testDestination` operation. */
    interface TestDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateVerifyDestination` operation. */
    interface UpdateVerifyDestinationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        /** Verification type. */
        type: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createTagsSubscription` operation. */
    interface CreateTagsSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        /** Unique identifier of the device. */
        deviceId: string;
        /** The name of the tag its subscribed. */
        tagName: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listTagsSubscription` operation. */
    interface ListTagsSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        /** Device ID of the destination tagsubscription. */
        deviceId?: string;
        /** UserID of the destination. */
        userId?: string;
        /** TagName of the subscription. */
        tagName?: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteTagsSubscription` operation. */
    interface DeleteTagsSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Destination. */
        id: string;
        /** Device ID of the destination tagsubscription. */
        deviceId?: string;
        /** TagName of the subscription. */
        tagName?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createSubscription` operation. */
    interface CreateSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Subscription name. */
        name: string;
        /** Destination ID. */
        destinationId: string;
        /** Topic ID. */
        topicId: string;
        /** Subscription description. */
        description?: string;
        attributes?: SubscriptionCreateAttributes;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listSubscriptions` operation. */
    interface ListSubscriptionsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** offset for paginated results. */
        offset?: number;
        /** Page limit for paginated results. */
        limit?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getSubscription` operation. */
    interface GetSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Subscription. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteSubscription` operation. */
    interface DeleteSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Subscription. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateSubscription` operation. */
    interface UpdateSubscriptionParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for Subscription. */
        id: string;
        /** Name of the subscription. */
        name?: string;
        /** Description of the subscription. */
        description?: string;
        attributes?: SubscriptionUpdateAttributes;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createIntegration` operation. */
    interface CreateIntegrationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** The type of Integration. */
        type: CreateIntegrationConstants.Type | string;
        /** Integration Metadata object. */
        metadata: IntegrationCreateMetadata;
        headers?: OutgoingHttpHeaders;
    }
    /** Constants for the `createIntegration` operation. */
    namespace CreateIntegrationConstants {
        /** The type of Integration. */
        enum Type {
            COLLECT_FAILED_EVENTS = "collect_failed_events"
        }
    }
    /** Parameters for the `listIntegrations` operation. */
    interface ListIntegrationsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** offset for paginated results. */
        offset?: number;
        /** Page limit for paginated results. */
        limit?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getIntegration` operation. */
    interface GetIntegrationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for integration. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `replaceIntegration` operation. */
    interface ReplaceIntegrationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for integration. */
        id: string;
        /** Integration type. Allowed values are kms, hs-crypto and collect_failed_events. */
        type: string;
        /** Integration Metadata object. */
        metadata: IntegrationMetadata;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createSmtpConfiguration` operation. */
    interface CreateSmtpConfigurationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** The name of SMTP configuration. */
        name: string;
        /** Domain Name. */
        domain: string;
        /** The description of SMTP configuration. */
        description?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listSmtpConfigurations` operation. */
    interface ListSmtpConfigurationsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `createSmtpUser` operation. */
    interface CreateSmtpUserParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** The description of SMTP configuration. */
        description?: string;
        /** provide name of the user to clone. */
        usernameToClone?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `listSmtpUsers` operation. */
    interface ListSmtpUsersParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** Page limit for paginated results. */
        limit?: number;
        /** offset for paginated results. */
        offset?: number;
        /** Search string for filtering results. */
        search?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getSmtpConfiguration` operation. */
    interface GetSmtpConfigurationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateSmtpConfiguration` operation. */
    interface UpdateSmtpConfigurationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** SMTP name. */
        name?: string;
        /** SMTP description. */
        description?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteSmtpConfiguration` operation. */
    interface DeleteSmtpConfigurationParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getSmtpUser` operation. */
    interface GetSmtpUserParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** UserID. */
        userId: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateSmtpUser` operation. */
    interface UpdateSmtpUserParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** UserID. */
        userId: string;
        /** SMTP user description. */
        description?: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `deleteSmtpUser` operation. */
    interface DeleteSmtpUserParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** UserID. */
        userId: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getSmtpAllowedIps` operation. */
    interface GetSmtpAllowedIpsParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `updateVerifySmtp` operation. */
    interface UpdateVerifySmtpParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Unique identifier for SMTP. */
        id: string;
        /** SMTP verification type. */
        type: string;
        headers?: OutgoingHttpHeaders;
    }
    /** Parameters for the `getNotificationsStatus` operation. */
    interface GetNotificationsStatusParams {
        /** Unique identifier for IBM Cloud Event Notifications instance. */
        instanceId: string;
        /** Get Notification Id. */
        id: string;
        headers?: OutgoingHttpHeaders;
    }
    /*************************
     * model interfaces
     ************************/
    /** Bucket object. */
    interface Buckets {
        /** Total count. */
        doc_count?: number;
        /** Timestamp. */
        key_as_string?: string;
    }
    /** Payload describing a Slack Direct Message chennel configuration. */
    interface ChannelCreateAttributes {
        /** channel id. */
        id: string;
    }
    /** Payload describing a Slack Direct Message chennel configuration. */
    interface ChannelUpdateAttributes {
        /** channel id. */
        id: string;
        /** The channel operation type. */
        operation: string;
    }
    /** The DKIM attributes. */
    interface DKIMAttributes {
        /** dkim public key. */
        public_key?: string;
        /** dkim selector. */
        selector?: string;
        /** dkim verification. */
        verification?: string;
    }
    /** Payload describing a destination get request. */
    interface Destination {
        /** Destination ID. */
        id: string;
        /** Destination name. */
        name: string;
        /** Destination description. */
        description: string;
        /** Destination type
         *  Email/SMS/Webhook/FCM/Slack/MSTeams/PagerDuty/IBMCodeEngine/ServiceNow/IBMCloudObjectStorage/Huawei/CustomEmail/CustomSMS/EventStreams.
         */
        type: string;
        /** Whether to collect the failed event in Cloud Object Storage bucket. */
        collect_failed_events?: boolean;
        /** Payload describing a destination configuration. */
        config?: DestinationConfig;
        /** Last updated time. */
        updated_at: string;
        /** Number of subscriptions. */
        subscription_count: number;
        /** List of subscriptions. */
        subscription_names: string[];
    }
    /** Payload describing a destination configuration. */
    interface DestinationConfig {
        params: DestinationConfigOneOf;
    }
    /** DestinationConfigOneOf. */
    interface DestinationConfigOneOf {
    }
    /** Payload describing a destination list request. */
    interface DestinationList {
        /** Total number of destinations. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show destinations. */
        limit: number;
        /** List of destinations. */
        destinations: DestinationListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Destination object. */
    interface DestinationListItem {
        /** Destination ID. */
        id: string;
        /** Destination name. */
        name: string;
        /** Destination description. */
        description: string;
        /** Destination type. */
        type: string;
        /** Whether to collect the failed event in Cloud Object Storage bucket. */
        collect_failed_events?: boolean;
        /** Subscription count. */
        subscription_count: number;
        /** Names of subscriptions. */
        subscription_names: string[];
        /** Updated at. */
        updated_at: string;
    }
    /** Payload describing a destination get request. */
    interface DestinationResponse {
        /** Destination ID. */
        id: string;
        /** Destination name. */
        name: string;
        /** Destination description. */
        description: string;
        /** Destination type. */
        type: string;
        /** Whether to collect the failed event in Cloud Object Storage bucket. */
        collect_failed_events?: boolean;
        /** Payload describing a destination configuration. */
        config: DestinationConfig;
        /** Last updated time. */
        created_at: string;
    }
    /** Payload describing a destination get request. */
    interface DestinationTagsSubscriptionResponse {
        /** Subscription Tag ID. */
        id: string;
        /** Unique identifier of the device. */
        device_id: string;
        /** The name of the tag its subscribed. */
        tag_name: string;
        /** The user identifier for the device registration. */
        user_id?: string;
        /** Last updated time. */
        created_at: string;
    }
    /** The en_authorization attributes. */
    interface ENAuthAttributes {
        /** en_authorization verification. */
        verification?: string;
    }
    /** EmailAttributesResponseInvitedItems. */
    interface EmailAttributesResponseInvitedItems {
        /** email address. */
        email?: string;
        /** last updated time. */
        updated_at?: string;
        /** time of expiration. */
        expires_at?: string;
    }
    /** EmailAttributesResponseSubscribedUnsubscribedItems. */
    interface EmailAttributesResponseSubscribedUnsubscribedItems {
        /** email address. */
        email?: string;
        /** last updated time. */
        updated_at?: string;
    }
    /** Payload describing a custom SMS Configuration. */
    interface EnabledCountriesResponse {
        /** The SMS destination status. */
        status: string;
        /** List enabled countries. */
        enabled_countries: SMSCountryConfig[];
    }
    /** Event schedule filter attributes. */
    interface EventScheduleFilterAttributes {
        /** event schedule start time. */
        starts_at?: string;
        /** event schedule end time. */
        ends_at?: string;
        /** cron schedule expression. */
        expression?: string;
    }
    /** Payload describing a notifications response. */
    interface GetNotificationStatusResponse {
        /** status of the notification. */
        status: string;
        /** Payload describing a Status Details. */
        details: GetNotificationStatusResponseDetails;
    }
    /** Payload describing a Status Details. */
    interface GetNotificationStatusResponseDetails {
        /** HTTP status code. */
        status_code: number;
        /** status message of the notification. */
        status_message: string;
    }
    /** Template object. */
    interface GetPredefinedTemplate {
        /** Template ID. */
        id: string;
        /** Template name. */
        name: string;
        /** Template description. */
        description: string;
        /** The type of template. */
        type: string;
        /** The type of source. */
        source?: string;
        /** Updated at. */
        updated_at: string;
        /** Payload describing a Predefined template configuration. */
        params: PredefinedTemplateConfig;
    }
    /** Payload describing histogram. */
    interface Histrogram {
        /** List of buckets. */
        buckets?: Buckets[];
    }
    /** Integration Metadata object. */
    interface IntegrationCreateMetadata {
        /** URL for Cloud Object storage. */
        endpoint: string;
        /** CRN of the Cloud Object Storage instance. */
        crn: string;
        /** Cloud Object Storage bucket name. */
        bucket_name: string;
    }
    /** Integration create response object. */
    interface IntegrationCreateResponse {
        /** ID of the integration. */
        id: string;
        /** Integration type. Allowed values collect_failed_events. */
        type: string;
        /** Integration Metadata object. */
        metadata: IntegrationCreateMetadata;
        /** Creation time of an integration. */
        created_at: string;
    }
    /** Integration response object. */
    interface IntegrationGetResponse {
        /** ID of the integration. */
        id: string;
        /** Integration type. Allowed values are kms and hs-crypto. */
        type: string;
        /** Integration Metadata object. */
        metadata: IntegrationMetadata;
        /** Creation time of an integration. */
        created_at: string;
        /** Last update time of an integration. */
        updated_at: string;
    }
    /** all Integrations response object. */
    interface IntegrationList {
        /** Number of integrations. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show integrations. */
        limit: number;
        /** List of integrations. */
        integrations: IntegrationListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** all Integrations response object. */
    interface IntegrationListItem {
        /** ID of the integration. */
        id: string;
        /** Integration type. Allowed values are kms and hs-crypto. */
        type: string;
        /** Integration Metadata object. */
        metadata: IntegrationMetadata;
        /** Creation time of an integration. */
        created_at: string;
        /** Update time of an integration. */
        updated_at: string;
    }
    /** Integration Metadata object. */
    interface IntegrationMetadata {
        /** KMS url for key management or url for COS bucket. */
        endpoint: string;
        /** CRN of the KMS/COS instance. */
        crn: string;
        /** Root Key ID of KMS. */
        root_key_id?: string;
        /** cloud object storage bucket name. */
        bucket_name?: string;
    }
    /** Payload describing metrics request. */
    interface Metric {
        /** key. */
        key?: string;
        /** doc count. */
        doc_count?: number;
        /** Payload describing histogram. */
        histogram?: Histrogram;
    }
    /** Payload describing a metrics. */
    interface Metrics {
        /** array of metrics. */
        metrics: Metric[];
    }
    /** Payload describing a notification create request. */
    interface NotificationCreate {
        /** The version of the notification specification. */
        specversion: string;
        /** The time notification was created. */
        time?: string;
        /** The unique identifier of the notification. */
        id: string;
        /** The source of notifications. */
        source: string;
        /** The notifications type. */
        type: string;
        /** The severity of the notification. */
        ibmenseverity?: string;
        /** The source id of the notification. */
        ibmensourceid: string;
        /** Default short text for the message. */
        ibmendefaultshort: string;
        /** Default long text for the message. */
        ibmendefaultlong: string;
        /** The subject of the notification. */
        ibmensubject?: string;
        /** The template id Array of string. */
        ibmentemplates?: string;
        /** The email id string. */
        ibmenmailto?: string;
        /** The slack channel id/member id stringified array. */
        ibmenslackto?: string;
        /** The SMS text. */
        ibmensmstext?: string;
        /** The SMS number string. */
        ibmensmsto?: string;
        /** The html body of notification. */
        ibmenhtmlbody?: string;
        /** The subject of the notification. */
        subject?: string;
        /** Stringified MMS Attachment JSON. */
        ibmenmms?: string;
        /** The markdown content for notification. */
        ibmenmarkdown?: string;
        /** The payload for webhook notification. */
        data?: JsonObject;
        /** The notification content type. */
        datacontenttype?: string;
        /** If platforms or tags or user_ids is used then do not use fcm_devices / apns_devices / chrome_devices /
         *  firefox_devices / safari_devices with it. Value should be stringified.
         */
        ibmenpushto?: string;
        /** Payload describing a notification FCM body. Value should be stringified. */
        ibmenfcmbody?: string;
        /** Payload describing a notification APNs body. Value should be stringified. */
        ibmenapnsbody?: string;
        /** Headers for iOS notification. Value should be stringified. */
        ibmenapnsheaders?: string;
        /** Notification payload for Chrome. Value should be stringified. */
        ibmenchromebody?: string;
        /** Headers for a Chrome notification. Value should be stringified. */
        ibmenchromeheaders?: string;
        /** Notification payload for Firefox. Value should be stringified. */
        ibmenfirefoxbody?: string;
        /** Headers for a Firefox notification. Value should be stringified. */
        ibmenfirefoxheaders?: string;
        /** Payload describing a notification Huawei body. Value should be stringified. */
        ibmenhuaweibody?: string;
        /** Payload describing a notification Safari body. Value should be stringified. */
        ibmensafaribody?: string;
        /** NotificationCreate accepts additional properties. */
        [propName: string]: any;
    }
    /** Payload describing a notifications response. */
    interface NotificationResponse {
        /** Notification ID. */
        notification_id?: string;
    }
    /** Response having URL of the page. */
    interface PageHrefResponse {
        /** URL to the page. */
        href: string;
    }
    /** Predefined Template object. */
    interface PredefinedTemplate {
        /** Template ID. */
        id: string;
        /** Template name. */
        name: string;
        /** Template description. */
        description: string;
        /** The type of source. */
        source: string;
        /** The type of template. */
        type: string;
        /** Updated at. */
        updated_at: string;
    }
    /** Payload describing a Predefined template configuration. */
    interface PredefinedTemplateConfig {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a pre-defined templates list request. */
    interface PredefinedTemplatesList {
        /** Total number of pre-defined templates. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show templates. */
        limit: number;
        /** List of pre-defined templates. */
        templates: PredefinedTemplate[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Rule object. */
    interface Rules {
        /** Whether the rule is enabled or not. */
        enabled?: boolean;
        /** Event type filter. */
        event_type_filter?: string;
        /** Notification filter. */
        notification_filter?: string;
        /** Event schedule filter attributes. */
        event_schedule_filter?: EventScheduleFilterAttributes;
    }
    /** Rule object. */
    interface RulesGet {
        /** Whether the rule is enabled or not. */
        enabled: boolean;
        /** Event type filter. */
        event_type_filter?: string;
        /** Notification filter. */
        notification_filter?: string;
        /** Event schedule filter attributes. */
        event_schedule_filter?: EventScheduleFilterAttributes;
        /** Last time the topic was updated. */
        updated_at: string;
        /** Autogenerated rule ID. */
        id: string;
    }
    /** The sms attributes. */
    interface SMSAttributesItems {
        /** Phone number. */
        phone_number?: string;
        /** last updated time. */
        updated_at?: string;
    }
    /** Payload describing a country Configuration. */
    interface SMSCountryConfig {
        /** Phone number. */
        number: string;
        /** List of Countries. */
        country: string[];
    }
    /** The sms attributes. */
    interface SMSInviteAttributesItems {
        /** Phone number. */
        phone_number?: string;
        /** last updated time. */
        updated_at?: string;
        /** time of expiration. */
        expires_at?: string;
    }
    /** Payload describing a SMTP allowed Ips. */
    interface SMTPAllowedIPs {
        /** The SMTP allowed Ips. */
        subnets: string[];
        /** Updated at. */
        updated_at: string;
    }
    /** Payload describing a SMTP configuration. */
    interface SMTPConfig {
        /** The SMTP DKIM attributes. */
        dkim?: SMTPDKIMAttributes;
        /** The en_authorization attributes. */
        en_authorization?: ENAuthAttributes;
        /** The SPF attributes. */
        spf?: SPFAttributes;
    }
    /** Payload describing a SMTP List response. */
    interface SMTPConfiguration {
        /** SMTP ID. */
        id: string;
        /** SMTP name. */
        name: string;
        /** SMTP description. */
        description?: string;
        /** Domain Name. */
        domain: string;
        /** Payload describing a SMTP configuration. */
        config: SMTPConfig;
        /** Created time. */
        updated_at: string;
    }
    /** Payload describing a SMTP Configurations list. */
    interface SMTPConfigurationsList {
        /** Total number of SMTP configurations. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show configurations. */
        limit: number;
        /** List of SMTP Configurations. */
        smtp_configurations: SMTPConfiguration[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Payload describing a SMTP create response. */
    interface SMTPCreateResponse {
        /** SMTP ID. */
        id: string;
        /** SMTP name. */
        name: string;
        /** SMTP description. */
        description?: string;
        /** Domain Name. */
        domain: string;
        /** Payload describing a SMTP configuration. */
        config: SMTPConfig;
        /** Created time. */
        created_at: string;
    }
    /** The SMTP DKIM attributes. */
    interface SMTPDKIMAttributes {
        /** DKIM text name. */
        txt_name?: string;
        /** DKIM text value. */
        txt_value?: string;
        /** DKIM verification. */
        verification?: string;
    }
    /** Payload describing a SMTP User. */
    interface SMTPUser {
        /** Id. */
        id: string;
        /** SMTP confg Id. */
        smtp_config_id: string;
        /** SMTP User description. */
        description: string;
        /** Domain Name. */
        domain: string;
        /** SMTP user name. */
        username: string;
        /** Updated time. */
        created_at: string;
        /** Updated time. */
        updated_at: string;
    }
    /** Payload describing a SMTP User create response. */
    interface SMTPUserResponse {
        /** SMTP Id. */
        id: string;
        /** SMTP User description. */
        description?: string;
        /** Domain Name. */
        domain?: string;
        /** SMTP confg Id. */
        smtp_config_id: string;
        /** SMTP user name. */
        username: string;
        /** Password for SMTP user; Cloned SMTP user response do not include a password. */
        password?: string;
        /** Created time. */
        created_at: string;
    }
    /** Payload describing a SMTP users list request. */
    interface SMTPUsersList {
        /** Total number of destinations. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show destinations. */
        limit: number;
        /** List of users. */
        users: SMTPUser[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** verification object. */
    interface SMTPVerificationResponse {
        /** verification type. */
        type: string;
        /** verification status. */
        verification: string;
    }
    /** Payload describing SMTP verification response. */
    interface SMTPVerificationUpdateResponse {
        /** SMTP verification status. */
        status: SMTPVerificationResponse[];
    }
    /** The SPF attributes. */
    interface SPFAttributes {
        /** spf text name. */
        txt_name?: string;
        /** spf text value. */
        txt_value?: string;
        /** spf verification. */
        verification?: string;
    }
    /** Payload describing a source generate request. */
    interface Source {
        /** The id of the source. */
        id: string;
        /** The name of the source. */
        name: string;
        /** The description of the source. */
        description: string;
        /** The status of the source. */
        enabled: boolean;
        /** Type of the source. */
        type: string;
        /** The last updated time of the source. */
        updated_at: string;
        /** The number of topics. */
        topic_count: number;
        /** The names of the topics. */
        topic_names: string[];
    }
    /** Payload describing a source list request. */
    interface SourceList {
        /** Number of sources. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show sources. */
        limit: number;
        /** List of sources. */
        sources: SourceListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Payload describing a source list item. */
    interface SourceListItem {
        /** ID of the source. */
        id: string;
        /** Name of the source. */
        name: string;
        /** Description of the source. */
        description: string;
        /** Type of the source. */
        type: string;
        /** Whether the source is enabled or not. */
        enabled: boolean;
        /** Time of the last update. */
        updated_at: string;
        /** Number of topics. */
        topic_count: number;
    }
    /** Payload describing a source. */
    interface SourceResponse {
        /** ID of the source. */
        id: string;
        /** Name of the source. */
        name: string;
        /** Description of the source. */
        description: string;
        /** Whether the source is enabled or not. */
        enabled: boolean;
        /** Time of the created. */
        created_at: string;
    }
    /** SourcesItems. */
    interface SourcesItems {
        /** ID of the source. */
        id: string;
        /** List of rules. */
        rules: Rules[];
    }
    /** Payload describing a source list item. */
    interface SourcesListItems {
        /** ID of the source. */
        id: string;
        /** Name of the source. */
        name: string;
        /** List of rules. */
        rules?: RulesGet[];
    }
    /** Subscription object. */
    interface Subscription {
        /** Subscription ID. */
        id: string;
        /** Subscription name. */
        name: string;
        /** Subscription description. */
        description: string;
        /** Last updated time. */
        updated_at: string;
        /** From Email ID (it will be displayed only in case of smtp_ibm destination type). */
        from?: string;
        /** The type of destination. */
        destination_type: string;
        /** The destination ID. */
        destination_id: string;
        /** The destination name. */
        destination_name: string;
        /** Topic ID. */
        topic_id: string;
        /** Topic name. */
        topic_name: string;
        attributes?: SubscriptionAttributes;
        /** Subscription accepts additional properties. */
        [propName: string]: any;
    }
    /** SubscriptionAttributes. */
    interface SubscriptionAttributes {
        /** SubscriptionAttributes accepts additional properties. */
        [propName: string]: any;
    }
    /** SubscriptionCreateAttributes. */
    interface SubscriptionCreateAttributes {
    }
    /** Subscription list object. */
    interface SubscriptionList {
        /** Number of subscriptions. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show subscriptions. */
        limit: number;
        /** List of subscriptions. */
        subscriptions: SubscriptionListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Subscription list item. */
    interface SubscriptionListItem {
        /** ID of the subscription. */
        id: string;
        /** Name of the subscription. */
        name: string;
        /** Description of the subscription. */
        description: string;
        /** ID of the destination. */
        destination_id: string;
        /** Name of the destination. */
        destination_name?: string;
        /** The type of destination. */
        destination_type: string;
        /** ID of the topic. */
        topic_id: string;
        /** Name of the topic. */
        topic_name?: string;
        /** Last updated time of the subscription. */
        updated_at: string;
    }
    /** SubscriptionUpdateAttributes. */
    interface SubscriptionUpdateAttributes {
    }
    /** Payload describing a tags list request. */
    interface TagsSubscriptionList {
        /** Total number of tags. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show tags. */
        limit: number;
        /** List of tags. */
        tag_subscriptions: TagsSubscriptionListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Tags subscription object. */
    interface TagsSubscriptionListItem {
        /** Subscription Tag ID. */
        id: string;
        /** Unique identifier of the device. */
        device_id: string;
        /** The name of the tag its subscribed. */
        tag_name: string;
        /** The user identifier for the device registration. */
        user_id?: string;
        /** Updated at. */
        updated_at: string;
    }
    /** Template object. */
    interface Template {
        /** Template ID. */
        id: string;
        /** Template name. */
        name: string;
        /** Template description. */
        description: string;
        /** The type of template. */
        type: string;
        /** Subscription count. */
        subscription_count: number;
        /** Names of subscriptions. */
        subscription_names: string[];
        /** Updated at. */
        updated_at: string;
    }
    /** TemplateConfigOneOf. */
    interface TemplateConfigOneOf {
    }
    /** Payload describing a template list request. */
    interface TemplateList {
        /** Total number of templates. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show templates. */
        limit: number;
        /** List of templates. */
        templates: Template[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Payload describing a template get request. */
    interface TemplateResponse {
        /** Template ID. */
        id: string;
        /** Template name. */
        name: string;
        /** Template description. */
        description?: string;
        /** The type of template. */
        type: string;
        params: TemplateConfigOneOf;
        /** Created time. */
        created_at: string;
    }
    /** TestDestinationResponse. */
    interface TestDestinationResponse {
    }
    /** Topic object. */
    interface Topic {
        /** Autogenerated topic ID. */
        id: string;
        /** Description of the topic. */
        description: string;
        /** Name of the topic. */
        name: string;
        /** Last time the topic was updated. */
        updated_at: string;
        /** Number of sources. */
        source_count: number;
        /** List of sources. */
        sources: SourcesListItems[];
        /** Number of subscriptions. */
        subscription_count: number;
        /** List of subscriptions. */
        subscriptions: SubscriptionListItem[];
    }
    /** Topic list object. */
    interface TopicList {
        /** Number of topics. */
        total_count: number;
        /** Current offset. */
        offset: number;
        /** limit to show subscriptions. */
        limit: number;
        /** List of topics. */
        topics: TopicsListItem[];
        /** Response having URL of the page. */
        first?: PageHrefResponse;
        /** Response having URL of the page. */
        previous?: PageHrefResponse;
        /** Response having URL of the page. */
        next?: PageHrefResponse;
    }
    /** Topic object. */
    interface TopicResponse {
        /** Autogenerated topic ID. */
        id: string;
        /** Name of the topic. */
        name: string;
        /** Description of the topic. */
        description: string;
        /** Last time the topic was updated. */
        created_at: string;
    }
    /** Topic list item object. */
    interface TopicsListItem {
        /** Autogenerated topic ID. */
        id: string;
        /** Name of the topic. */
        name: string;
        /** Description of the topic. */
        description: string;
        /** Number of sources. */
        source_count: number;
        /** List of source names. */
        sources_names: string[];
        /** Number of subscriptions. */
        subscription_count: number;
    }
    /** The email ids or phone numbers. */
    interface UpdateAttributesInvited {
        /** The email ids or phone numbers to be invited. */
        add?: string[];
        /** The email ids or phone numbers for removal. */
        remove?: string[];
    }
    /** The email ids or phone numbers. */
    interface UpdateAttributesSubscribed {
        /** The email ids or phone numbers to be unsubscribed. */
        remove?: string[];
    }
    /** The email ids or phone numbers. */
    interface UpdateAttributesUnsubscribed {
        /** The email ids or phone numbers to be unsubscribed. */
        remove?: string[];
    }
    /** Destination verification object. */
    interface VerificationResponse {
        /** verification type. */
        type: string;
        /** verification status. */
        verification: string;
    }
    /** Payload describing a App Configuration destination configuration. */
    interface DestinationConfigOneOfAppConfigurationDestinationConfig extends DestinationConfigOneOf {
        /** The App Configuration Destination type. */
        type: string;
        /** CRN of the App Configuration instance. */
        crn: string;
        /** Environment ID of App Configuration. */
        environment_id: string;
        /** Feature ID of App Configuration. */
        feature_id: string;
    }
    /** Payload describing a Chrome destination configuration. */
    interface DestinationConfigOneOfChromeDestinationConfig extends DestinationConfigOneOf {
        /** FCM api_key. */
        api_key: string;
        /** Website url. */
        website_url: string;
        /** Chrome VAPID public key. */
        public_key?: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
    }
    /** Payload describing a code engine destination configuration. */
    interface DestinationConfigOneOfCodeEngineDestinationConfig extends DestinationConfigOneOf {
        /** URL of code engine. */
        url?: string;
        /** HTTP method of code engine. */
        verb?: string;
        /** The code engine destination type. */
        type: string;
        /** CRN of the code engine project. */
        project_crn?: string;
        /** name of the code engine job. */
        job_name?: string;
        /** Custom headers (Key-Value pair) for webhook call. */
        custom_headers?: JsonObject;
        /** List of sensitive headers from custom headers. */
        sensitive_headers?: string[];
    }
    /** Payload describing a custom Domain Email destination configuration. */
    interface DestinationConfigOneOfCustomDomainEmailDestinationConfig extends DestinationConfigOneOf {
        /** Email Domain. */
        domain: string;
        /** The DKIM attributes. */
        dkim?: DKIMAttributes;
        /** The SPF attributes. */
        spf?: SPFAttributes;
    }
    /** Payload describing a Event Streams destination configuration. */
    interface DestinationConfigOneOfEventStreamsDestinationConfig extends DestinationConfigOneOf {
        /** CRN of the Event Streans instance. */
        crn: string;
        /** Endpoint of Event Streams. */
        endpoint: string;
        /** Topic of Event Streams. */
        topic: string;
    }
    /** Payload describing an FCM destination configuration. project_id, private_key and client_email for FCM HTTP v1 APIs. */
    interface DestinationConfigOneOfFCMDestinationConfig extends DestinationConfigOneOf {
        /** Deprecated: FCM server_key. */
        server_key?: string;
        /** Deprecated: FCM sender_id. */
        sender_id?: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
        /** FCM project_id. */
        project_id?: string;
        /** FCM private_key. */
        private_key?: string;
        /** FCM client_email. */
        client_email?: string;
    }
    /** Payload describing a Firefox destination configuration. */
    interface DestinationConfigOneOfFirefoxDestinationConfig extends DestinationConfigOneOf {
        /** Website url. */
        website_url: string;
        /** Chrome VAPID public key. */
        public_key?: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
    }
    /** Payload describing a Huawei destination configuration. */
    interface DestinationConfigOneOfHuaweiDestinationConfig extends DestinationConfigOneOf {
        /** ClientID for the Huawei account oauth. */
        client_id: string;
        /** ClientSecret for the Huawei account oauth. */
        client_secret: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
    }
    /** Payload describing a Cloud Object Storage destination configuration. */
    interface DestinationConfigOneOfIBMCloudObjectStorageDestinationConfig extends DestinationConfigOneOf {
        /** Bucket Name for Cloud Object Storage. */
        bucket_name: string;
        /** Instance Id of Cloud Object Storage. */
        instance_id: string;
        /** Endpoint of Cloud Object Storage. */
        endpoint: string;
    }
    /** Payload describing an iOS destination configuration. */
    interface DestinationConfigOneOfIOSDestinationConfig extends DestinationConfigOneOf {
        /** Authentication type (p8 or p12). */
        cert_type: string;
        /** Sandbox mode for iOS destinations. */
        is_sandbox: boolean;
        /** Password for certificate (Required when cert_type is p12). */
        password?: string;
        /** Key ID for token (Required when cert_type is p8). */
        key_id?: string;
        /** Team ID for token (Required when cert_type is p8). */
        team_id?: string;
        /** Bundle ID for token (Required when cert_type is p8). */
        bundle_id?: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
    }
    /** Payload describing a MS Teams destination configuration. */
    interface DestinationConfigOneOfMSTeamsDestinationConfig extends DestinationConfigOneOf {
        /** URL of MS Teams Incoming Notifications. */
        url: string;
    }
    /** Payload describing a PagerDuty destination configuration. */
    interface DestinationConfigOneOfPagerDutyDestinationConfig extends DestinationConfigOneOf {
        /** Deprecated: API Key for the PagerDuty account. */
        api_key?: string;
        /** Routing Key (Integration Key) for the team in PagerDuty account. */
        routing_key: string;
    }
    /** Payload describing a Safari destination configuration. */
    interface DestinationConfigOneOfSafariDestinationConfig extends DestinationConfigOneOf {
        /** Authentication type p12. */
        cert_type: string;
        /** Password for certificate (Required when cert_type is p12). */
        password: string;
        /** Website url. */
        website_url: string;
        /** Website url. */
        website_name: string;
        /** Website url. */
        url_format_string: string;
        /** Website url. */
        website_push_id: string;
        /** If pre prod enabled. */
        pre_prod?: boolean;
    }
    /** Payload describing a ServiceNow destination configuration. */
    interface DestinationConfigOneOfServiceNowDestinationConfig extends DestinationConfigOneOf {
        /** ClientID for the ServiceNow account oauth. */
        client_id: string;
        /** ClientSecret for the ServiceNow account oauth. */
        client_secret: string;
        /** Username for ServiceNow account REST API. */
        username: string;
        /** Password for ServiceNow account REST API. */
        password: string;
        /** Instance name for ServiceNow account. */
        instance_name: string;
    }
    /** Payload describing a Slack webhook destination configuration. */
    interface DestinationConfigOneOfSlackDestinationConfig extends DestinationConfigOneOf {
        /** URL of Slack Incoming Notifications. */
        url: string;
        /** The Slack Destination type. */
        type: string;
    }
    /** Payload describing a Slack direct message destination configuration. */
    interface DestinationConfigOneOfSlackDirectMessageDestinationConfig extends DestinationConfigOneOf {
        /** Token of slack application. */
        token: string;
        /** The Slack Destination type. */
        type: string;
    }
    /** Payload describing a webhook destination configuration. */
    interface DestinationConfigOneOfWebhookDestinationConfig extends DestinationConfigOneOf {
        /** URL of webhook. */
        url: string;
        /** HTTP method of webhook. */
        verb: string;
        /** Custom headers (Key-Value pair) for webhook call. */
        custom_headers?: JsonObject;
        /** List of sensitive headers from custom headers. */
        sensitive_headers?: string[];
    }
    /** The attributes for a App Configuration notification. */
    interface SubscriptionAttributesAppConfigurationAttributesResponse extends SubscriptionAttributes {
        /** App Configuration enable feature flag attribute. */
        feature_flag_enabled?: boolean;
        /** App Configuration template id. */
        template_id_notification?: string;
    }
    /** The attributes for a Code Engine response. */
    interface SubscriptionAttributesCodeEngineAttributesResponse extends SubscriptionAttributes {
        /** Code Engine template id. */
        template_id_notification?: string;
    }
    /** The attributes reponse for an email destination. */
    interface SubscriptionAttributesCustomEmailAttributesResponse extends SubscriptionAttributes {
        /** The email id string. */
        invited?: EmailAttributesResponseInvitedItems[];
        /** The unsubscribe list. */
        subscribed?: EmailAttributesResponseSubscribedUnsubscribedItems[];
        /** The subscribed list. */
        unsubscribed?: EmailAttributesResponseSubscribedUnsubscribedItems[];
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
        /** The email from. */
        from_email: string;
        /** The templete id for notification. */
        template_id_notification?: string;
        /** The templete id for invitation. */
        template_id_invitation?: string;
    }
    /** Custom SMS attributes object. */
    interface SubscriptionAttributesCustomSMSAttributesResponse extends SubscriptionAttributes {
        /** The subscribed list. */
        subscribed?: SMSAttributesItems[];
        /** The unsubscribe list. */
        unsubscribed?: SMSAttributesItems[];
        /** The SMS number string. */
        invited?: SMSInviteAttributesItems[];
    }
    /** The attributes reponse for an email destination. */
    interface SubscriptionAttributesEmailAttributesResponse extends SubscriptionAttributes {
        /** The email id string. */
        invited?: EmailAttributesResponseInvitedItems[];
        /** The unsubscribe list. */
        subscribed?: EmailAttributesResponseSubscribedUnsubscribedItems[];
        /** The subscribed list. */
        unsubscribed?: EmailAttributesResponseSubscribedUnsubscribedItems[];
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
    }
    /** The attributes for a Event Streams response. */
    interface SubscriptionAttributesEventStreamsAttributesResponse extends SubscriptionAttributes {
        /** Event Streams template id. */
        template_id_notification?: string;
    }
    /** The attributes for a PagerDuty notification. */
    interface SubscriptionAttributesPagerDutyAttributesResponse extends SubscriptionAttributes {
        /** ID of Base64 converted JSON Pagerduty Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** SMS attributes object. */
    interface SubscriptionAttributesSMSAttributesResponse extends SubscriptionAttributes {
        /** The subscribed list. */
        subscribed?: SMSAttributesItems[];
        /** The unsubscribe list. */
        unsubscribed?: SMSAttributesItems[];
        /** The SMS numder string. */
        invited?: SMSInviteAttributesItems[];
    }
    /** The attributes for a ServiceNow notification. */
    interface SubscriptionAttributesServiceNowAttributesResponse extends SubscriptionAttributes {
        /** Assigned name from ServiceNow account. */
        assigned_to?: string;
        /** Assigned group name from ServiceNow account. */
        assignment_group?: string;
    }
    /** The attributes for a Slack notification. */
    interface SubscriptionAttributesSlackAttributesResponse extends SubscriptionAttributes {
        /** Attachment Color for Slack Notification. */
        attachment_color?: string;
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a slack direct message. */
    interface SubscriptionAttributesSlackDirectMessageAttributesResponse extends SubscriptionAttributes {
        /** List of channels. */
        channels?: ChannelCreateAttributes[];
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a webhook notification. */
    interface SubscriptionAttributesWebhookAttributesResponse extends SubscriptionAttributes {
        /** Signing webhook attributes. */
        signing_enabled: boolean;
        /** Decision for Notification Payload to be added. */
        add_notification_payload: boolean;
    }
    /** The attributes for a App Configuration subscription. */
    interface SubscriptionCreateAttributesAppConfigurationAttributes extends SubscriptionCreateAttributes {
        /** App Configuration enable feature flag attribute. */
        feature_flag_enabled?: boolean;
        /** App Configuration template id. */
        template_id_notification?: string;
    }
    /** The attributes for a Code Engine subscription. */
    interface SubscriptionCreateAttributesCodeEngineAttributes extends SubscriptionCreateAttributes {
        /** code engine template id. */
        template_id_notification?: string;
    }
    /** The attributes for an email notification. */
    interface SubscriptionCreateAttributesCustomEmailAttributes extends SubscriptionCreateAttributes {
        /** The email id string. */
        invited: string[];
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
        /** The email from. */
        from_email: string;
        /** The templete id for notification. */
        template_id_notification?: string;
        /** The templete id for invitation. */
        template_id_invitation?: string;
    }
    /** The attributes for an custom sms notification. */
    interface SubscriptionCreateAttributesCustomSMSAttributes extends SubscriptionCreateAttributes {
        /** The sms id string. */
        invited: string[];
    }
    /** The attributes for an email notification. */
    interface SubscriptionCreateAttributesEmailAttributes extends SubscriptionCreateAttributes {
        /** The email id string. */
        invited: string[];
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
    }
    /** The attributes for a Event Streams subscription. */
    interface SubscriptionCreateAttributesEventstreamsAttributes extends SubscriptionCreateAttributes {
        /** Event Streams template id. */
        template_id_notification?: string;
    }
    /** The attributes for an FCM notification. */
    interface SubscriptionCreateAttributesFCMAttributes extends SubscriptionCreateAttributes {
    }
    /** The attributes for a pagerduty notification. */
    interface SubscriptionCreateAttributesPagerDutyAttributes extends SubscriptionCreateAttributes {
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for an sms notification. */
    interface SubscriptionCreateAttributesSMSAttributes extends SubscriptionCreateAttributes {
        /** The sms id string. */
        invited: string[];
    }
    /** The attributes for a ServiceNow notification. */
    interface SubscriptionCreateAttributesServiceNowAttributes extends SubscriptionCreateAttributes {
        /** Name of user ServiceNow incident will be assigned to. */
        assigned_to?: string;
        /** Group Name to which incident will be assigned to. */
        assignment_group?: string;
    }
    /** The attributes for a slack notification. */
    interface SubscriptionCreateAttributesSlackAttributes extends SubscriptionCreateAttributes {
        /** Attachment Color for the slack message. */
        attachment_color?: string;
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a slack direct message. */
    interface SubscriptionCreateAttributesSlackDirectMessageAttributes extends SubscriptionCreateAttributes {
        /** List of channels. */
        channels?: ChannelCreateAttributes[];
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a webhook notification. */
    interface SubscriptionCreateAttributesWebhookAttributes extends SubscriptionCreateAttributes {
        /** Signing webhook attributes. */
        signing_enabled?: boolean;
        /** ID of Base64 converted JSON webhook Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a App Configuration subscription. */
    interface SubscriptionUpdateAttributesAppConfigurationAttributes extends SubscriptionUpdateAttributes {
        /** App Configuration enable feature flag attribute. */
        feature_flag_enabled?: boolean;
        /** App Configuration template id. */
        template_id_notification?: string;
    }
    /** The attributes for a Code Engine subscription. */
    interface SubscriptionUpdateAttributesCodeEngineAttributes extends SubscriptionUpdateAttributes {
        /** code engine template id. */
        template_id_notification?: string;
    }
    /** The attributes for an email notification. */
    interface SubscriptionUpdateAttributesCustomEmailUpdateAttributes extends SubscriptionUpdateAttributes {
        /** The email ids or phone numbers. */
        invited?: UpdateAttributesInvited;
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
        /** The email from. */
        from_email: string;
        /** The email ids or phone numbers. */
        subscribed?: UpdateAttributesSubscribed;
        /** The email ids or phone numbers. */
        unsubscribed?: UpdateAttributesUnsubscribed;
        /** The templete id for notification. */
        template_id_notification?: string;
        /** The templete id for invitation. */
        template_id_invitation?: string;
    }
    /** SMS attributes object. */
    interface SubscriptionUpdateAttributesCustomSMSUpdateAttributes extends SubscriptionUpdateAttributes {
        /** The email ids or phone numbers. */
        invited?: UpdateAttributesInvited;
        /** The email ids or phone numbers. */
        subscribed?: UpdateAttributesSubscribed;
        /** The email ids or phone numbers. */
        unsubscribed?: UpdateAttributesUnsubscribed;
    }
    /** The attributes for an email notification. */
    interface SubscriptionUpdateAttributesEmailUpdateAttributes extends SubscriptionUpdateAttributes {
        /** The email ids or phone numbers. */
        invited?: UpdateAttributesInvited;
        /** Whether to add the notification payload to the email. */
        add_notification_payload: boolean;
        /** The email address to reply to. */
        reply_to_mail: string;
        /** The email name to reply to. */
        reply_to_name: string;
        /** The email name of From. */
        from_name: string;
        /** The email ids or phone numbers. */
        subscribed?: UpdateAttributesSubscribed;
        /** The email ids or phone numbers. */
        unsubscribed?: UpdateAttributesUnsubscribed;
    }
    /** The attributes for a Event Streams subscription. */
    interface SubscriptionUpdateAttributesEventstreamsAttributes extends SubscriptionUpdateAttributes {
        /** Event Streams template id. */
        template_id_notification?: string;
    }
    /** The attributes for a pagerduty notification. */
    interface SubscriptionUpdateAttributesPagerDutyAttributes extends SubscriptionUpdateAttributes {
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** SMS attributes object. */
    interface SubscriptionUpdateAttributesSMSUpdateAttributes extends SubscriptionUpdateAttributes {
        /** The email ids or phone numbers. */
        invited?: UpdateAttributesInvited;
        /** The email ids or phone numbers. */
        subscribed?: UpdateAttributesSubscribed;
        /** The email ids or phone numbers. */
        unsubscribed?: UpdateAttributesUnsubscribed;
    }
    /** The attributes for a ServiceNow notification. */
    interface SubscriptionUpdateAttributesServiceNowAttributes extends SubscriptionUpdateAttributes {
        /** Name of user ServiceNow incident will be assigned to. */
        assigned_to?: string;
        /** Group Name to which incident will be assigned to. */
        assignment_group?: string;
    }
    /** The attributes for a slack notification. */
    interface SubscriptionUpdateAttributesSlackAttributes extends SubscriptionUpdateAttributes {
        /** Attachment Color for the slack message. */
        attachment_color?: string;
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a slack direct message. */
    interface SubscriptionUpdateAttributesSlackDirectMessageUpdateAttributes extends SubscriptionUpdateAttributes {
        /** List of channels. */
        channels?: ChannelUpdateAttributes[];
        /** ID of Base64 converted JSON Slack Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** The attributes for a webhook notification. */
    interface SubscriptionUpdateAttributesWebhookAttributes extends SubscriptionUpdateAttributes {
        /** Signing webhook attributes. */
        signing_enabled?: boolean;
        /** ID of Base64 converted JSON webhook Blocks w/o Handlebars. */
        template_id_notification?: string;
    }
    /** Payload describing a App Configuration template configuration. */
    interface TemplateConfigOneOfAppConfigurationTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a code engine application template configuration. */
    interface TemplateConfigOneOfCodeEngineApplicationTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a code engine Job template configuration. */
    interface TemplateConfigOneOfCodeEngineJobTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing an email template configuration. */
    interface TemplateConfigOneOfEmailTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
        /** The template subject. */
        subject?: string;
    }
    /** Payload describing a event streams template configuration. */
    interface TemplateConfigOneOfEventStreamsTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a pagerduty template configuration. */
    interface TemplateConfigOneOfPagerdutyTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a slack template configuration. */
    interface TemplateConfigOneOfSlackTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Payload describing a webhook template configuration. */
    interface TemplateConfigOneOfWebhookTemplateConfig extends TemplateConfigOneOf {
        /** Template body(Base64 encoded). */
        body: string;
    }
    /** Test Destination response object for other http destination. */
    interface TestDestinationResponseDestinationTestResponseConfig extends TestDestinationResponse {
        /** Test destination status. */
        status: string;
    }
    /** Response from /test endpoint. */
    interface TestDestinationResponseWebhookDestinationTestResponseConfig extends TestDestinationResponse {
        /** Test destination status. */
        status: string;
        /** Test notification ID. */
        notification_id: string;
        /** Type of destination. */
        destination_type: string;
    }
    /*************************
     * pager classes
     ************************/
    /**
     * SourcesPager can be used to simplify the use of listSources().
     */
    class SourcesPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListSourcesParams;
        /**
         * Construct a SourcesPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listSources()
         * @param {Object} params - The parameters to be passed to listSources()
         * @constructor
         * @returns {SourcesPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListSourcesParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listSources().
         * @returns {Promise<EventNotificationsV1.SourceListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.SourceListItem[]>;
        /**
         * Returns all results by invoking listSources() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.SourceListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.SourceListItem[]>;
    }
    /**
     * TopicsPager can be used to simplify the use of listTopics().
     */
    class TopicsPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListTopicsParams;
        /**
         * Construct a TopicsPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listTopics()
         * @param {Object} params - The parameters to be passed to listTopics()
         * @constructor
         * @returns {TopicsPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListTopicsParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listTopics().
         * @returns {Promise<EventNotificationsV1.TopicsListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.TopicsListItem[]>;
        /**
         * Returns all results by invoking listTopics() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.TopicsListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.TopicsListItem[]>;
    }
    /**
     * TemplatesPager can be used to simplify the use of listTemplates().
     */
    class TemplatesPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListTemplatesParams;
        /**
         * Construct a TemplatesPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listTemplates()
         * @param {Object} params - The parameters to be passed to listTemplates()
         * @constructor
         * @returns {TemplatesPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListTemplatesParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listTemplates().
         * @returns {Promise<EventNotificationsV1.Template[]>}
         */
        getNext(): Promise<EventNotificationsV1.Template[]>;
        /**
         * Returns all results by invoking listTemplates() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.Template[]>}
         */
        getAll(): Promise<EventNotificationsV1.Template[]>;
    }
    /**
     * PreDefinedTemplatesPager can be used to simplify the use of listPreDefinedTemplates().
     */
    class PreDefinedTemplatesPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListPreDefinedTemplatesParams;
        /**
         * Construct a PreDefinedTemplatesPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listPreDefinedTemplates()
         * @param {Object} params - The parameters to be passed to listPreDefinedTemplates()
         * @constructor
         * @returns {PreDefinedTemplatesPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListPreDefinedTemplatesParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listPreDefinedTemplates().
         * @returns {Promise<EventNotificationsV1.PredefinedTemplate[]>}
         */
        getNext(): Promise<EventNotificationsV1.PredefinedTemplate[]>;
        /**
         * Returns all results by invoking listPreDefinedTemplates() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.PredefinedTemplate[]>}
         */
        getAll(): Promise<EventNotificationsV1.PredefinedTemplate[]>;
    }
    /**
     * DestinationsPager can be used to simplify the use of listDestinations().
     */
    class DestinationsPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListDestinationsParams;
        /**
         * Construct a DestinationsPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listDestinations()
         * @param {Object} params - The parameters to be passed to listDestinations()
         * @constructor
         * @returns {DestinationsPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListDestinationsParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listDestinations().
         * @returns {Promise<EventNotificationsV1.DestinationListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.DestinationListItem[]>;
        /**
         * Returns all results by invoking listDestinations() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.DestinationListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.DestinationListItem[]>;
    }
    /**
     * TagsSubscriptionPager can be used to simplify the use of listTagsSubscription().
     */
    class TagsSubscriptionPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListTagsSubscriptionParams;
        /**
         * Construct a TagsSubscriptionPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listTagsSubscription()
         * @param {Object} params - The parameters to be passed to listTagsSubscription()
         * @constructor
         * @returns {TagsSubscriptionPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListTagsSubscriptionParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listTagsSubscription().
         * @returns {Promise<EventNotificationsV1.TagsSubscriptionListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.TagsSubscriptionListItem[]>;
        /**
         * Returns all results by invoking listTagsSubscription() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.TagsSubscriptionListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.TagsSubscriptionListItem[]>;
    }
    /**
     * SubscriptionsPager can be used to simplify the use of listSubscriptions().
     */
    class SubscriptionsPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListSubscriptionsParams;
        /**
         * Construct a SubscriptionsPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listSubscriptions()
         * @param {Object} params - The parameters to be passed to listSubscriptions()
         * @constructor
         * @returns {SubscriptionsPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListSubscriptionsParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listSubscriptions().
         * @returns {Promise<EventNotificationsV1.SubscriptionListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.SubscriptionListItem[]>;
        /**
         * Returns all results by invoking listSubscriptions() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.SubscriptionListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.SubscriptionListItem[]>;
    }
    /**
     * IntegrationsPager can be used to simplify the use of listIntegrations().
     */
    class IntegrationsPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListIntegrationsParams;
        /**
         * Construct a IntegrationsPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listIntegrations()
         * @param {Object} params - The parameters to be passed to listIntegrations()
         * @constructor
         * @returns {IntegrationsPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListIntegrationsParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listIntegrations().
         * @returns {Promise<EventNotificationsV1.IntegrationListItem[]>}
         */
        getNext(): Promise<EventNotificationsV1.IntegrationListItem[]>;
        /**
         * Returns all results by invoking listIntegrations() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.IntegrationListItem[]>}
         */
        getAll(): Promise<EventNotificationsV1.IntegrationListItem[]>;
    }
    /**
     * SmtpConfigurationsPager can be used to simplify the use of listSmtpConfigurations().
     */
    class SmtpConfigurationsPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListSmtpConfigurationsParams;
        /**
         * Construct a SmtpConfigurationsPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listSmtpConfigurations()
         * @param {Object} params - The parameters to be passed to listSmtpConfigurations()
         * @constructor
         * @returns {SmtpConfigurationsPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListSmtpConfigurationsParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listSmtpConfigurations().
         * @returns {Promise<EventNotificationsV1.SMTPConfiguration[]>}
         */
        getNext(): Promise<EventNotificationsV1.SMTPConfiguration[]>;
        /**
         * Returns all results by invoking listSmtpConfigurations() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.SMTPConfiguration[]>}
         */
        getAll(): Promise<EventNotificationsV1.SMTPConfiguration[]>;
    }
    /**
     * SmtpUsersPager can be used to simplify the use of listSmtpUsers().
     */
    class SmtpUsersPager {
        protected _hasNext: boolean;
        protected pageContext: any;
        protected client: EventNotificationsV1;
        protected params: EventNotificationsV1.ListSmtpUsersParams;
        /**
         * Construct a SmtpUsersPager object.
         *
         * @param {EventNotificationsV1}  client - The service client instance used to invoke listSmtpUsers()
         * @param {Object} params - The parameters to be passed to listSmtpUsers()
         * @constructor
         * @returns {SmtpUsersPager}
         */
        constructor(client: EventNotificationsV1, params: EventNotificationsV1.ListSmtpUsersParams);
        /**
         * Returns true if there are potentially more results to be retrieved by invoking getNext().
         * @returns {boolean}
         */
        hasNext(): boolean;
        /**
         * Returns the next page of results by invoking listSmtpUsers().
         * @returns {Promise<EventNotificationsV1.SMTPUser[]>}
         */
        getNext(): Promise<EventNotificationsV1.SMTPUser[]>;
        /**
         * Returns all results by invoking listSmtpUsers() repeatedly until all pages of results have been retrieved.
         * @returns {Promise<EventNotificationsV1.SMTPUser[]>}
         */
        getAll(): Promise<EventNotificationsV1.SMTPUser[]>;
    }
}
export = EventNotificationsV1;
